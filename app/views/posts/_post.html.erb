<div id="<%= dom_id post %>">
  
<div class="container">
  <h2><%= post.title %></h2>
  <h4>Posted by <%= link_to post.user.name, user_path(post.user) %></h4>
  <h6><%= pluralize(post.view, "view") %></h6>
  <div>
  <%= post.body %>
  </div>
</div>
</div>
